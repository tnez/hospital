= form_tag search_patients_path, class: "navbar-form", role: "search", remote: true  do
  .input-group
    = text_field_tag 'q', params[:q], class: "form-control", id: "srch-term", placeholder: "Search"
    .input-group-btn
      %button.btn.btn-default{:style => "width:40px;"}
        -# %i.glyphicon.glyphicon-search

%h2 Patient List
%ul.patient-list.paginate
  = render 'patient_list', collection: @patients

